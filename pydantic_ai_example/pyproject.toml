[project]
name = "pydantic-ai-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "create-agent-app",
    "langwatch-scenario>=0.1.1",
    "pydantic-ai>=1.99.0",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
]

[tool.uv.sources]
create-agent-app = { path = "../", editable = true }
